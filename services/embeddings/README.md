# Embeddings Service

Reserved boundary for sentence-transformers and Qdrant integration.

The first implementation should expose a small pipeline:

```text
text -> chunk -> embed -> store/search
```
