# ML Service

Reserved for TensorFlow experiments, training scripts, and exported classifiers.

Add TensorFlow and training dependencies to `apps/api/environment.yml` only when this service becomes active.
